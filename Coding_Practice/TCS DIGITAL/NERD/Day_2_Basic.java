package NERD;

public class Day_2_Basic {

    public static void main(String[]args){
        String s1="Indian";
        String s2=" Oil";
        String s3=s1.concat(s2);
        System.out.println(s3);

        String s4=30+50+"string"+40+40;
        System.out.println(s4);

        //toString Method
        //charAt method

        //java.lang.String

        //toCharArray method to convert string to character array

        //String Comparision using three methods
        //equals, equalsIgnoreCase methods
        //s1.equals(s2)  s1.equals
        //s1==s2
        String s10=new String("Atria");
        String s9="Atria";
        System.out.print(s9==s10);

        //startsWith, endsWith, (toCompare methof returns the lexicographical order)
        //indexOf will return the index of first occurence
        //lastIndexOf will return the index of last occurence
        //indexOf('is',4) will start searching after index 4
        

        //Substring in Java
        //substring is subset of another string, startindex is inclusive and endIndex is exclusive
        //substring(int startIndex) substring(startindex,endindex)


        //trim() method
        //toLowerCase()
        //toUpperCase()

        //StringBuffer append method, replace method, reverse method
        //StringBuilder class
        
    }
}
